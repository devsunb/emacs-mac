# emacs-mac 개인 포크

실사용할 Emacs를 안정적이고 빠르게, 최소 유지 부담으로 굴리는 것이 목적이다.
업스트림 PR은 목표가 아니고 머지 가능 품질도 불필요하다.

로컬 패치는 upstream 위의 두 스택이다. 구성은 `docs/local.md`를 본다.

- main: 지금 실제로 겪는 문제를 고치는 커밋만 둔다.
- dev: 가까운 장래에 겪을 법한 것을 파킹한다. main 위가 아니라 upstream 위에 얹힌 독립 스택이다.
- 어느 쪽에도 해당하지 않으면 두지 않는다.

결함을 발견하면 현재 사용 조건에서 도달하는지 먼저 따진다.
도달하지 않으면 보고만 하고 고치지 않는다.
설정으로 해결되는 것은 커밋 대신 init으로 옮긴다.
업스트림에 같은 수정이 들어오면 커밋을 그대로 버릴 수 있어야 한다.
그래서 diff에는 코드만 두고, 커밋 메시지는 트리거와 변경 요약까지, 소스 주석은 코드를 고칠 때 깨지기 쉬운 제약까지만 담는다.
근거, 범위, 검증, 커밋 사이의 의존과 스택 위치는 커밋 메시지가 아니라 `docs/local.md`에 적는다.
담을지 말지 갈리면 diff에서 빼고 `docs/local.md`에 적는다.
같은 문장을 diff와 `docs/local.md` 양쪽에 두지 않는다. 주석이 제약을 적으면 문서에는 그 근거와 한계만 적는다.
